mixin radio-button(items)
  .radio-button
    each item in items
      label.radio-button__title= item.text
        input.radio-button__input(
          type="radio"
          name= item.name
          value= item.value
          checked= item.checked
        )
        .radio-button__checkmark